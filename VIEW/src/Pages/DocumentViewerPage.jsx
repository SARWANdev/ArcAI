import "./DocumentViewerPage.css"
/**
 * DocumentViewerPage is the page that is used to display the document.
 * @returns {JSX} - The React component for the document viewer page.
 */
export default function DocumentViewerPage(){
    return(
        <div>
            <h1>Document Viewer</h1>
        </div>
    )
}