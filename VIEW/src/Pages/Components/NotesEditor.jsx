import "./NotesEditor.css"
/**
 * NotesEditor is the component that is used to display the notes editor in the document viewer page.
 * @returns {JSX} - The React component for the notes editor.
 */
export default function NotesEditor(){
    return(
        <div className="notes-editor-container">
            <h1>Notes Editor</h1>
        </div>
    )
}