import "./ClearAllButton.css"
/**
 * ClearAllButton is the component that is used to display the clear all button in the chat history page.
 * @returns {JSX} - The React component for the clear all button.
 */
export default function ClearAllButton(){
    return(
        <button className="clear-all-button">
            Clear All
        </button>
    )
}