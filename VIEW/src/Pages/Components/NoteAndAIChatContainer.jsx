import "./NoteAndAIChatContainer.css"
/**
 * NoteAndAIChatContainer is the component that is used to display the note and AI chat container in the document viewer page.
 * @returns {JSX} - The React component for the note and AI chat container.
 */
export default function NoteAndAIChatContainer(){
    return(
        <div className="note-and-ai-chat-container">
            <h1>NoteAndAIChatContainer</h1>
        </div>
    )
}